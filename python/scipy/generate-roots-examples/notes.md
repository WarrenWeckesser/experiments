Also add Examples section to the eval_* function docstrings.

These could
(1) Make a simple at a few points and show the result.
(2) Use quad to demonstrate orthogonality.
(3) Show that the values at the root returned by roots_* are in fact
    (approximately) 0. (It's OK that this repeats the demonstation
    from the roots_* docstrings.  This is documentation, not code.)
